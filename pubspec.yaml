name: gvg_job_scraper
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_spinkit: ^5.2.0
  http: ^1.1.0
  multi_dropdown: ^2.1.0
  url_launcher: ^6.2.1

dev_dependencies:
  flutter_lints: ^2.0.0
  dependency_validator: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/alfred.json
    - assets/tvinna.txt
