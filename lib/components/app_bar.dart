import 'package:flutter/material.dart';

// The AppBar to be used for all pages
class GlobalAppBar extends AppBar {
  GlobalAppBar({super.key})
      : super(
          title: const Text('JobScraper'),
        );
}
